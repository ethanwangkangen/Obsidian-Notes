```cpp
int main(int argc, char* argv[]) {
}
```
- argc (argument count)
	- integer parameter of count of number of arguments passed to program
	- at least 1, since first is always program name
- argv (argument vectors/values)
	- array of strings, each string is argument

Converting string to int
- `std::stringstream convert{ argv[1] };`