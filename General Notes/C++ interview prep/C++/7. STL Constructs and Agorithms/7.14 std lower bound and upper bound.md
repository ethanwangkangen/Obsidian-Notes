```cpp
// lower_bound calls comp(element, value)
// → first element for which comp(elem, value) is FALSE
auto it1 = std::lower_bound(v.begin(), v.end(), value, comp);

// upper_bound calls comp(value, element)
// → first element for which comp(value, elem) is TRUE
auto it2 = std::upper_bound(v.begin(), v.end(), value, comp);
```